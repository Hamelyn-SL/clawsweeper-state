---
number: 7269
repository: hamelyn-sl/hamelyn-serverless
title: "feat: distribute the hamelyn cli over http with self-update"
source_report: records/hamelyn-sl-hamelyn-serverless/items/7269.md
reviewed_at: 2026-07-09T08:11:06.281Z
work_candidate: queue_fix_pr
work_priority: high
work_confidence: high
---

# Coding Plan for hamelyn-sl/hamelyn-serverless#7269: feat: distribute the hamelyn cli over http with self-update

Source report: [records/hamelyn-sl-hamelyn-serverless/items/7269.md](records/hamelyn-sl-hamelyn-serverless/items/7269.md)

## Summary

Debe seguir abierto: el PR implementa la feature enlazada, pero introduce riesgos reparables en el instalador Windows y en el workflow de publicación que ejecuta código npm mutable con un token de escritura.

## Plan

En el PR de distribución HTTP del Hamelyn CLI, arregla solo los bloqueos de revisión: 1) reemplaza `npx --yes vercel@54` en `.github/workflows/cli-release.yml` por una versión exacta/verificada o una invocación controlada que no resuelva código npm mutable en runtime con `CLI_BLOB_READ_WRITE_TOKEN`; 2) corrige `apps/api/public/cli/install.ps1` para que si el PATH de usuario está vacío escriba únicamente `$BinDir`, y solo use `;$BinDir` cuando ya haya contenido; 3) regenera o corrige `apps/hamelyn-cli/Cargo.lock` para que los paquetes locales coincidan con la versión `0.4.0` del workspace. No cambies la dirección funcional del canal HTTP, no vuelvas a Homebrew, no añadas nuevas features y no edites `CHANGELOG.md`. Valida con checks focalizados de Rust/CLI si están disponibles y con revisión estática del workflow/script.

## Likely Files

- `.github/workflows/cli-release.yml`
- `apps/api/public/cli/install.ps1`
- `apps/hamelyn-cli/Cargo.lock`

## Validation

- `cargo test --workspace desde apps/hamelyn-cli, si el entorno Rust está disponible`
- `cargo fmt --check desde apps/hamelyn-cli, si el entorno Rust está disponible`
- `Revisión estática de \`.github/workflows/cli-release.yml\` para confirmar versión exacta/controlada de la herramienta de publicación`
- `Revisión estática de \`apps/api/public/cli/install.ps1\` para \`Path\` vacío y \`Path\` existente`

## Cluster References

- https://github.com/Hamelyn-SL/hamelyn-serverless/issues/7264

## Notes

- This file is generated dashboard state from the durable review report.
- Regenerate it from the source report instead of editing it by hand.
