---
number: 7904
repository: hamelyn-sl/hamelyn-serverless
title: "🐛 API v6 no se despliega automáticamente desde dev ni master"
source_report: records/hamelyn-sl-hamelyn-serverless/items/7904.md
reviewed_at: 2026-07-23T09:46:21.086Z
work_candidate: queue_fix_pr
work_priority: high
work_confidence: high
---

# Coding Plan for hamelyn-sl/hamelyn-serverless#7904: 🐛 API v6 no se despliega automáticamente desde dev ni master

Source report: [records/hamelyn-sl-hamelyn-serverless/items/7904.md](records/hamelyn-sl-hamelyn-serverless/items/7904.md)

## Summary

La incidencia sigue siendo necesaria: la configuración actual desactiva los despliegues Git de Vercel y el pipeline de GitHub Actions no conoce ningún target `API` ni sus hooks.

## Plan

Corrige el despliegue automático de `apps/api` sin reactivar los Git deployments de Vercel. Añade `API` a `.github/workflows/deploy.yml` como target manual y expón `API_DEV` y `API_PROD`; añade `api`/`API` a `apps/local/scripts/deploy_projects.ts`, incluidos los cambios raíz y de `packages/core` que afectan al runtime; incorpora cobertura focalizada del enrutado si la infraestructura existente lo permite; actualiza la documentación de despliegue. No cambies otros destinos ni modifiques `CHANGELOG.md`. Verifica formato, lint del workspace local y ejecuciones dirigidas contra `dev` y `master` con hooks de prueba configurados.

## Likely Files

- `.github/workflows/deploy.yml`
- `apps/local/scripts/deploy_projects.ts`
- `skills/hamelyn-knowledge/platform/02-deployment.md`

## Validation

- `pnpm exec prettier --check .github/workflows/deploy.yml apps/local/scripts/deploy_projects.ts skills/hamelyn-knowledge/platform/02-deployment.md`
- `pnpm --filter local lint`
- `Ejecutar \`workflow_dispatch\` con \`target=API\` en \`dev\` y en \`master\`, confirmando la aceptación y el estado final del deployment en Vercel.`

## Cluster References

- none

## Notes

- This file is generated dashboard state from the durable review report.
- Regenerate it from the source report instead of editing it by hand.
