---
number: 6689
repository: hamelyn-sl/hamelyn-serverless
title: "crons: /api/milanuncios-responder falla con fetch failed antes de cargar mensajes"
source_report: records/hamelyn-sl-hamelyn-serverless/items/6689.md
reviewed_at: 2026-07-14T15:57:22.027Z
work_candidate: queue_fix_pr
work_priority: medium
work_confidence: high
---

# Coding Plan for hamelyn-sl/hamelyn-serverless#6689: crons: /api/milanuncios-responder falla con fetch failed antes de cargar mensajes

Source report: [records/hamelyn-sl-hamelyn-serverless/items/6689.md](records/hamelyn-sl-hamelyn-serverless/items/6689.md)

## Summary

Conviene mantenerla abierta: `dev` sigue conservando el problema central de observabilidad. El trabajo posterior en https://github.com/Hamelyn-SL/hamelyn-serverless/pull/7418 cubrió únicamente los timeouts conocidos; un `fetch failed` no temporizado continúa propagándose sin indicar la fase y acaba convertido por el wrapper compartido en una respuesta HTTP 200.

## Plan

Corrige la incidencia de `/api/milanuncios-responder` en un único PR focalizado. En `apps/crons/src/controller/milanuncios/index.ts`, conserva el diferimiento actual de `TimeoutError`, pero envuelve los demás rechazos de `fetch` con el nombre de la fase (`carga de login`, `login` o `activación de sesión`) y preserva la causa original. En `apps/crons/src/app/api/milanuncios-responder/route.ts`, sustituye el agregador `cronHandler` por una ejecución directa mediante `withCronRoute` que devuelva 200 solo cuando `processMilanunciosMessages` complete o difiera de forma conocida, y permita que los errores no clasificados produzcan 500. No cambies la semántica compartida de `packages/core/src/cron-jobs/cron-handler-app-router.ts`, los reintentos de `getPendingMessages`, el presupuesto temporal ni el contenido funcional de las respuestas automáticas. Verifica tanto un timeout conocido como un `TypeError: fetch failed` controlado y confirma en la salida que el primero se difiere sin archivar conversaciones mientras el segundo incluye la fase y devuelve 500. No versiones scripts one-time; documenta la ejecución dirigida en el PR.

## Likely Files

- `apps/crons/src/controller/milanuncios/index.ts`
- `apps/crons/src/app/api/milanuncios-responder/route.ts`

## Validation

- `pnpm --filter crons lint`
- `pnpm --filter crons exec next typegen`
- `pnpm --filter crons exec tsc --noEmit --pretty false`
- `Ejecución dirigida local con \`fetch\` controlado: comprobar diferimiento para \`TimeoutError\` y HTTP 500 con contexto de fase para \`TypeError: fetch failed\`, sin versionar el script de verificación.`

## Cluster References

- https://github.com/Hamelyn-SL/hamelyn-serverless/issues/7416
- https://github.com/Hamelyn-SL/hamelyn-serverless/pull/7418
- https://github.com/Hamelyn-SL/hamelyn-serverless/issues/6007

## Notes

- This file is generated dashboard state from the durable review report.
- Regenerate it from the source report instead of editing it by hand.
