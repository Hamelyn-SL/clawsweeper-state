---
number: 7748
repository: hamelyn-sl/hamelyn-serverless
title: "Cron de Amazon Returns Pendings en no-op silencioso desde su creación: el gate de hora exacta nunca se cumple"
source_report: records/hamelyn-sl-hamelyn-serverless/items/7748.md
reviewed_at: 2026-07-20T12:33:04.618Z
work_candidate: queue_fix_pr
work_priority: high
work_confidence: high
---

# Coding Plan for hamelyn-sl/hamelyn-serverless#7748: Cron de Amazon Returns Pendings en no-op silencioso desde su creación: el gate de hora exacta nunca se cumple

Source report: [records/hamelyn-sl-hamelyn-serverless/items/7748.md](records/hamelyn-sl-hamelyn-serverless/items/7748.md)

## Summary

Gracias por el contexto: el defecto sigue presente en `dev` y no hay un PR abierto o fix integrado que lo sustituya. La discusión ya concreta una corrección pequeña y operativa.

## Plan

Corrige el cron de Amazon Returns Pendings para que cada `schedule` ejecute exactamente una sincronización diaria sin depender de la hora de arranque del runner. En `.github/workflows/amazon-returns-pendings.yml`, deja un único cron a las 05:00 UTC y elimina la lógica de `--force` condicionada al dispatch. En `.github/scripts/amazon-returns-pendings.ts`, elimina el gate `getMadridHour`/`shouldRunNow` y su salida silenciosa, conservando `--hours` y el manejo de errores actual. Valida formato y la estructura del workflow; no añadas scripts one-time ni modifiques `CHANGELOG.md`. En el PR, documenta una ejecución manual real o un run programado posterior con secretos redactados.

## Likely Files

- `.github/workflows/amazon-returns-pendings.yml`
- `.github/scripts/amazon-returns-pendings.ts`

## Validation

- `pnpm exec prettier --check .github/workflows/amazon-returns-pendings.yml .github/scripts/amazon-returns-pendings.ts`
- `pnpm exec tsx .github/scripts/amazon-returns-pendings.ts --hours=24`
- `Confirmar en una ejecución de GitHub Actions que un evento \`schedule\` inicia \`Starting Amazon returns pending sync\` y no imprime el mensaje de skip.`

## Cluster References

- https://github.com/Hamelyn-SL/hamelyn-serverless/pull/7743

## Notes

- This file is generated dashboard state from the durable review report.
- Regenerate it from the source report instead of editing it by hand.
