---
number: 7623
repository: hamelyn-sl/hamelyn-serverless
title: "Errores en el monedero al añadir productos"
source_report: records/hamelyn-sl-hamelyn-serverless/items/7623.md
reviewed_at: 2026-07-16T08:52:03.922Z
work_candidate: queue_fix_pr
work_priority: high
work_confidence: high
---

# Coding Plan for hamelyn-sl/hamelyn-serverless#7623: Errores en el monedero al añadir productos

Source report: [records/hamelyn-sl-hamelyn-serverless/items/7623.md](records/hamelyn-sl-hamelyn-serverless/items/7623.md)

## Summary

El problema sigue presente en `dev`: el cliente permite que una nueva interacción empiece mientras el alta anterior puede seguir pendiente y el repositorio guarda el documento completo mediante lectura-modificación-`save()`, una combinación capaz de perder productos concurrentes. No hay un PR abierto ni otro elemento canónico que ya esté resolviendo esta pérdida de actualizaciones.

## Plan

Corrige la pérdida intermitente de productos del monedero de Supply descrita en esta issue. En `apps/public/src/services/db-adapter/cart.repository.ts`, sustituye el alta basada en `findById` + modificación de `products` + `save()` por una operación MongoDB atómica que preserve todas las altas concurrentes, actualice `lastUpdatedTimestamp` y devuelva o permita recuperar de forma inequívoca el carrito resultante. Mantén los errores de dominio, el límite de unidades por EAN, las reglas de precio y las reglas de baja demanda actuales. En `apps/public/src/controllers/cart/cartId/product.ts`, usa el resultado confirmado sin introducir una lectura que pueda devolver un estado anterior. En `apps/supply/components/ProductPage/ProductContent.tsx` y las vistas de producto, evita envíos repetidos y no navegues al escáner hasta que la petición termine correctamente; actualiza total y contador desde la respuesta confirmada. Añade cobertura de regresión para dos o más altas concurrentes sobre el mismo carrito y verifica también el caso de carrito autenticado o fusionado. No cambies políticas de negocio, contratos no relacionados, ficheros nativos generados de Expo ni `CHANGELOG.md`; si el arreglo exige redefinir la fusión o los límites, detente y solicita revisión humana. La sección del PR debe documentar el comportamiento visible y la prueba concurrente ejecutada.

## Likely Files

- `apps/public/src/services/db-adapter/cart.repository.ts`
- `apps/public/src/controllers/cart/cartId/product.ts`
- `apps/supply/components/ProductPage/ProductContent.tsx`
- `apps/supply/components/ProductPage/ProductInfoDesktop.tsx`
- `apps/supply/components/ProductPage/ProductInfoMobile.tsx`

## Validation

- `pnpm exec prettier --check apps/public/src/services/db-adapter/cart.repository.ts apps/public/src/controllers/cart/cartId/product.ts apps/supply/components/ProductPage/ProductContent.tsx apps/supply/components/ProductPage/ProductInfoDesktop.tsx apps/supply/components/ProductPage/ProductInfoMobile.tsx`
- `pnpm --filter public lint`
- `pnpm --filter public exec next typegen`
- `pnpm --filter public exec tsc --noEmit --pretty false`
- `pnpm --filter supply lint`
- `Ejecutar una prueba dirigida con dos o más altas concurrentes sobre el mismo carrito y comprobar en la respuesta y en MongoDB que persisten todas las unidades`
- `Ejecutar el flujo de Supply añadiendo varios productos rápidamente y verificar que contador, total y contenido final permanecen estables`

## Cluster References

- none

## Notes

- This file is generated dashboard state from the durable review report.
- Regenerate it from the source report instead of editing it by hand.
