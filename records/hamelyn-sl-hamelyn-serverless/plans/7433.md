---
number: 7433
repository: hamelyn-sl/hamelyn-serverless
title: "Discogs withdrawal pipeline leaves fresh WITHDRAWN stock listed for sale"
source_report: records/hamelyn-sl-hamelyn-serverless/items/7433.md
reviewed_at: 2026-07-13T08:28:42.031Z
work_candidate: queue_fix_pr
work_priority: high
work_confidence: medium
---

# Coding Plan for hamelyn-sl/hamelyn-serverless#7433: Discogs withdrawal pipeline leaves fresh WITHDRAWN stock listed for sale

Source report: [records/hamelyn-sl-hamelyn-serverless/items/7433.md](records/hamelyn-sl-hamelyn-serverless/items/7433.md)

## Summary

Debe seguir abierta: el código actual de `dev` ya selecciona `mk_discogs` con `stockStatus=WITHDRAWN` y `status=ACTIVE`, pero el reporte aporta una muestra viva posterior en la API de Discogs, así que no hay evidencia suficiente para cerrarla como implementada.

## Plan

Arreglar la issue de Discogs en la que un stock `WITHDRAWN` con documento `mk_discogs` `status=ACTIVE` y `marketplaceId` puede seguir `For Sale` en la API. Investigar por qué `findStocksToWithdrawFromMarketplace(MarketplaceDatabase.DISCOGS, [music, vinyl], limit)` no consume filas frescas como `Happ-2025-12-30-8b35c477`: revisar límite/config `marketplaceBatch.discogs.withdraw`, ordenación por `lastUpdatedTimestamp`, persistencia tras `deleteListing`, tratamiento de `MarketplaceActiveAndError` en `enableSyncBatchWithdraw`, y carrera con `create/update/price` ejecutadas en paralelo en `discogsMk`. Mantener el cambio en Discogs o en helpers compartidos si la causa es genérica, añadir cobertura unitaria/focalizada para la selección y persistencia de retirada, y no tocar integraciones no relacionadas ni scripts one-time versionados. Validar con checks focalizados y, si hay credenciales operativas, con una ejecución/log redacted del flujo de withdraw sobre una muestra controlada.

## Likely Files

- `apps/marketplace-crons/src/controllers/discogs/index.ts`
- `packages/core/src/domain/marketplaces/marketplace.withdraw-product.ts`
- `packages/core/src/infrastructure/database/repositories/stocks.repository/queries.ts`
- `packages/core/src/infrastructure/database/repositories/marketplace.repository.ts`
- `packages/core/src/infrastructure/external-apis/discogs/api.ts`
- `packages/core/src/config/index.ts`

## Validation

- `pnpm --filter @hamelyn/core test -- packages/core/src/domain/marketplaces/marketplace.withdraw-product.test.ts`
- `pnpm --filter @hamelyn/core test -- packages/core/src/infrastructure/database/repositories/stocks.repository/queries.test.ts`
- `pnpm --filter marketplace-crons lint`

## Cluster References

- https://github.com/Hamelyn-SL/hamelyn-serverless/issues/7215

## Notes

- This file is generated dashboard state from the durable review report.
- Regenerate it from the source report instead of editing it by hand.
