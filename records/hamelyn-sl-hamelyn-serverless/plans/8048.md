---
number: 8048
repository: hamelyn-sl/hamelyn-serverless
title: "fix: derive watcher queue metrics from executable selectors and live config"
source_report: records/hamelyn-sl-hamelyn-serverless/items/8048.md
reviewed_at: 2026-07-30T10:58:10.262Z
work_candidate: queue_fix_pr
work_priority: high
work_confidence: high
---

# Coding Plan for hamelyn-sl/hamelyn-serverless#8048: fix: derive watcher queue metrics from executable selectors and live config

Source report: [records/hamelyn-sl-hamelyn-serverless/items/8048.md](records/hamelyn-sl-hamelyn-serverless/items/8048.md)

## Summary

Mantener abierto: el PR corrige una necesidad real de los watchers, pero `count: true` valida un plan con `sort` y `limit` distinto del `countDocuments` que ejecuta; además falta prueba visible contra Mongo/PROD antes de integrar.

## Plan

En la rama de este PR, corrige `apps/agents/agent/tools/mongo_find.ts` para que el guard de `count: true` inspeccione la misma forma de consulta que ejecutará `countDocuments`: solo el filtro, sin `sort`, `projection` ni `limit`. Endurece `resolveMarketplace` en `apps/agents/agent/tools/marketplace_batch_config.ts` para aceptar únicamente números finitos del contrato. Añade regresiones a los tests de tools, no cambies el comportamiento normal de lectura ni amplíes acceso a `config`, y no edites changelogs. Contexto canónico: https://github.com/Hamelyn-SL/hamelyn-serverless/issues/8040.

## Likely Files

- `apps/agents/agent/tools/mongo_find.ts`
- `apps/agents/agent/lib/tools/mongo_find.test.ts`
- `apps/agents/agent/tools/marketplace_batch_config.ts`
- `apps/agents/agent/lib/tools/marketplace_batch_config.test.ts`

## Validation

- `pnpm --filter agents test`
- `pnpm --filter agents lint`
- `pnpm --filter agents exec tsc --noEmit -p tsconfig.json`
- `pnpm --filter agents run build:eve`
- `Aportar una ejecución Mongo/PROD redactada con el plan, el guard y el resultado del conteo.`

## Cluster References

- https://github.com/Hamelyn-SL/hamelyn-serverless/issues/8040

## Notes

- This file is generated dashboard state from the durable review report.
- Regenerate it from the source report instead of editing it by hand.
