---
number: 6488
repository: hamelyn-sl/hamelyn-serverless
title: "supply: user_id y user_properties no llegan a GA4/BigQuery"
source_report: records/hamelyn-sl-hamelyn-serverless/items/6488.md
reviewed_at: 2026-07-07T23:30:45.204Z
work_candidate: queue_fix_pr
work_priority: medium
work_confidence: high
---

# Coding Plan for hamelyn-sl/hamelyn-serverless#6488: supply: user_id y user_properties no llegan a GA4/BigQuery

Source report: [records/hamelyn-sl-hamelyn-serverless/items/6488.md](records/hamelyn-sl-hamelyn-serverless/items/6488.md)

## Summary

Keep open: current `dev` still has the Auth0 provider-tree mismatch, so the GA4 `user_id` and custom user property setters remain wired to a pre-provider `useAuth()` consumer.

## Plan

Fix the Supply GA4 identity bug reported in https://github.com/Hamelyn-SL/hamelyn-serverless/issues/6488. In `apps/supply/app/_layout.tsx`, ensure the component that calls `useAuth()` for `setAnalyticsUser`, `setAnalyticsUserProperties`, token sync, and `trackSupplyAuthenticatedIdentity` is rendered as a descendant of `HamelynAuth0Provider`; prefer the minimal provider-tree change that also fixes `syncTokenAndIdentity`. Do not edit generated Expo native folders, do not add one-time scripts, and do not change the analytics event contract unless the existing contract cannot support the fix. Add or update focused coverage if the Supply workspace has a suitable harness; otherwise document manual validation for an internal Supply build, Firebase DebugView, and the BigQuery checks for `user_id` plus `app_language`, `app_version`, `platform`, and `auth_state`.

## Likely Files

- `apps/supply/app/_layout.tsx`
- `apps/supply/context/account.context.tsx`
- `apps/supply/services/analytics/index.ts`

## Validation

- `pnpm --filter supply lint`
- `Manual internal Supply build login smoke test with Firebase DebugView showing \`setUserId\` and the four user properties after authentication`
- `Post-release BigQuery query against \`hamelyn-data.analytics_325438134.events_*\` confirming post-login \`user_id\` and \`app_language\`, \`app_version\`, \`platform\`, \`auth_state\` in \`user_properties[]\``

## Cluster References

- https://github.com/Hamelyn-SL/hamelyn-serverless/issues/6488

## Notes

- This file is generated dashboard state from the durable review report.
- Regenerate it from the source report instead of editing it by hand.
